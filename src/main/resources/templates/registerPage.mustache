<!DOCTYPE html>
<html  xmlns:th="http://www.thymeleaf.org">
<head>
    <meta charset="UTF-8">
    <link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/4.4.1/css/bootstrap.min.css" integrity="sha384-Vkoo8x4CGsO3+Hhxv8T/Q5PaXtkKtu6ug5TOeNV6gBiFeWPGFN9MuhOf23Q9Ifjh" crossorigin="anonymous">
    <title> Регистрация </title>
</head>
    <body>
    <header th:insert= "blocks/header :: header.mustache">
        <div class="d-flex flex-column flex-md-row align-items-center p-3 px-md-4 mb-3 bg-white border-bottom shadow-sm">
                <a class="btn btn-primary" href="/registration">
                    <h5 class="my-0 mr-md-auto font-weight-normal">Cookies</h5>
                </a>
                <nav class="my-2 my-md-0 mr-md-3">
                    <a class="p-2 text-dark" href="#">Рецепты</a>
                    <a class="p-2 text-dark" href="#">О нас</a>
                </nav>
                <a class="btn btn-outline-primary" href="/registration">Войти</a>
            </div>
    </header>
        <div>
            <form method="post">
                <input type="text" name="login" placeholder="Введите логин" />
                <input type="text" name="name" placeholder="Введите имя пользователя" />
                <input type="text" name="secondName" placeholder="Фамилия пользователя" />
                <button type="submit">Добавить</button>
            </form>
        </div>
        {{#users}}
        <div class="my-md-2">
            <b>{{Id}}</b>
            <span>{{Login}}<span>
            <span>{{Name}}</span>
            <span>{{SecondName}}<span>
            <a class="btn btn-outline-primary" href="/user/{{Id}}" class="button">Профиль</a>
        </div>
        {{/users}}
    </body>
</html>